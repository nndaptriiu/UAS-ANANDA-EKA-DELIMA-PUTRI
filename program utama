def main() :
data_mahasiswa = []
nilai_akhir = []
grades = []

proses = Proses()
    tampilan = Tampilan()
    
    print("="*80)
    print("PROGRAM PENILAIAN MAHASISWA")
    print("="*80)
    
    while True:
        print("\n--- Input Data ---")
        nim = input("NIM   : ")
        nama = input("Nama  : ")
        
        try:
            tugas = validasi_nilai(input("Tugas : "))
            uts = validasi_nilai(input("UTS   : "))
            uas = validasi_nilai(input("UAS   : "))
